//1.1

const firstName = "Artūrs";
const lastName = "Līdums";
let age = 22;
const university = "VIA";
let yearOfStudy = "3rd";

console.log(firstName, lastName, age, university, yearOfStudy)

//1.2

let grade1 = 5;
let grade2 = 8;
let grade3 = 7;

const total = (grade1 + grade2 + grade3);
const avg = total / 3;

console.log(total, avg)

//1.3

let price = 80;
let quantity = 3;
let discount = 0.10;

const totalPrice = price * quantity;
const discountPrice = totalPrice * discount;
const finalPrice = totalPrice - discountPrice;

console.log(finalPrice) //216

//1.4

let a = 15;
let b = 4;

const remainder = a / b;
const raised = a ** b;
const greater = a > b;
const equal = a == b;

console.log(remainder, raised, greater, equal)

//2.1

const avgGrade = 88;

if (avgGrade >=90) {
    console.log("Excellent")
}
if(avgGrade >=80 && avgGrade <=89) {
    console.log("Very Good")
}
if (avgGrade >=70 && avgGrade <=79) {
    console.log("Good")
}
if (avgGrade >=50 && avgGrade <=69) {
    console.log("Pass")
}
if (avgGrade <50) {
    console.log("Fail")
}

//2.2

if (age >= 16 && age <=18) {
    console.log("Can recieve a student discount")
}
if (age >= 18) {
    console.log("Can vote")
}

//2.3

let username = "student";
let password = "js123";

if (username == "student" && password == "js123") {
    console.log("Login succesfull")
}
else {
 console.log("invalid credentials")
}

//2.4


// (==) Checks only the value. This can lead to unexpected or incorrect results.

// (===) Checks value and type. this will produce more correct ansver.

// Example

let num = 15;
let str = "15";

console.log(num == str);   // Outputs (true) because values match
console.log(num === str);  // Outputs (false) because types don't match (number vs string)

//3.1

const student = {
    firstName: firstName,
    lastName: lastName,
    age: age,
    studentId: "IT24067",
    program: "IT",
    year: yearOfStudy,
    averageGrade: 8
};

console.log(student);

//3.2

const student1 = {
    fullName: firstName + " " + lastName,
    program: "IT",
    year: yearOfStudy,
    averageGrade: 8
};

console.log(student1);

//3.3

student.avgGrade = 7;
student.year = 4
student.email = "arturs.lidums@va.lv";

console.log(student)

//3.4

student.avgGrade = 88;
student.getStatus = function() {
    if (this.avgGrade >= 90) {
        return "Excellent student";
    } 
    if (this.avgGrade >= 70 && this.avgGrade <= 89) {
        return "Good student";
    } 
    return "Needs improvement";
};

console.log(student.getStatus());

//4.1

const student2 = {
    firstName: firstName,
    lastName: lastName,
    age: age,
    program: "IT",
    averageGrade: 8,
    creditsCompleted: 95
};

console.log(student2)

//4.2

student.avgGrade = 88;

if (age >= 18 && avgGrade >= 50) {
    console.log("Registration Status: Eligible")
}
else {
    console.log("Registration Status: Not Eligible")
}

//4.3

student.avgGrade = 88;


let courseFee = 500;
let discount1 = 0;

if (student.avgGrade >= 90) {
    discount1 = 0.25;
} else if (student.avgGrade >= 80 && student.avgGrade <= 89) {
    discount1 = 0.15;
} else if (student.avgGrade >= 70 && student.avgGrade <= 79) {
    discount1 = 0.10;
}

const discountAmount = courseFee * discount1;
const finalFee = courseFee - discountAmount;

console.log("Final course fee:", finalFee);

//4.4

student.creditsCompleted = 88;

let studyLevel = "";

if (student.creditsCompleted < 30) {
    studyLevel = "Beginner";
} else if (student.creditsCompleted >= 30 && student.creditsCompleted <= 59) {
    studyLevel = "Intermediate";
} else if (student.creditsCompleted >= 60 && student.creditsCompleted <= 119) {
    studyLevel = "Advanced";
} else {
    studyLevel = "Graduating";
}


//4.5

console.log("\n----- STUDENT REPORT -----\n\nName Name: " + firstName + " " + lastName + "\nProgram: " + student.program + "\nAverage Grade: " + avgGrade + "\nEligibility: " + ((age >= 18 && student.avgGrade >= 50) ? "Eligible" : "Not Eligible") + "\nCourse Fee: " + finalFee + "\nStudy Level: " + studyLevel + "\n\n-------------------------- ")
